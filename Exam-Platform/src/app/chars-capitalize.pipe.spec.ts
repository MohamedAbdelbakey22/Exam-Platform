import { CharsCapitalizePipe } from './pipes/chars-capitalize.pipe';

describe('CharsCapitalizePipe', () => {
  it('create an instance', () => {
    const pipe = new CharsCapitalizePipe();
    expect(pipe).toBeTruthy();
  });
});
